{
  "name": "create-react-app-wasm",
  "version": "0.1.0",
  "scripts": {
    "build": "yarn --cwd web build",
    "start": "yarn --cwd web start",
    "test": "run-s test:*",
    "test:crate": "cd crate; make test",
    "test:web": "yarn --cwd web test"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
